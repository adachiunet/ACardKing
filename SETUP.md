# AcardKing(名片王)— 安裝與建置說明

> **這是「借一次實體 Mac」的路線。** 如果你不想借 Mac、想完全在 Windows 上用雲端編譯 + Sideloadly 簽名安裝,
> 改看同一個 zip 裡的 `CI-SETUP.md`——兩條路線效果完全一樣,選一條做就好。

這個資料夾是完整的 Swift 原始碼(Models / Services / Views / Utilities),**不是**一個現成的 `.xcodeproj`。
因為這個開發環境是雲端 Linux,沒有 Xcode 也沒有 macOS,沒辦法直接產生或驗證 `.xcodeproj` 專案檔本身,
所以採用最穩妥的方式:你在自己的 Mac 上用 Xcode 建一個全新的空白專案,再把這些檔案拖進去。這樣可以避免手刻
`.xcodeproj` 檔案格式(裡面是很容易壞掉的內部 ID 索引)造成專案打不開的風險。

## 前提

- 一台 Mac,裝 **Xcode 15 以上**(才有 iOS 17 SDK)。你目前沒有 Mac,需要自己找管道(借用/雲端 Mac 租用服務等)。
- 一支 iPhone,以及你的 Apple ID(免費帳號即可,不需要付費開發者帳號)。

## 步驟

1. **開新專案**:Xcode → File → New → Project → iOS → **App**。
   - Product Name:`AcardKing`——**這一步建議照打**,因為 Xcode 預設會拿 Product Name 當作
     「Bundle Display Name」,也就是裝到手機後主畫面圖示底下顯示的名字;App 內導覽列的
     「AcardKing」文字是另外用程式碼寫死的,兩邊已經對齊,Product Name 打別的名字也不會讓程式碼
     跑不動,只是主畫面圖示下面的字會跟 App 內看到的不一樣。
   - Interface:**SwiftUI**
   - Storage:**SwiftData**(這一步很重要,勾了 Xcode 才會自動幫你設定好 SwiftData 需要的 entitlement)
   - 「Host in CloudKit」**不要勾**——這個 App 設計上完全離線,不用雲端同步。

2. **刪除範本自動產生的檔案**:Xcode 用 SwiftData 範本會自動生成 `Item.swift` 和預設的 `ContentView.swift`。
   把這兩個檔案刪掉(Item.swift 整個刪;ContentView.swift 也刪,我們會用自己的 CardListView 當首頁)。

3. **把這個資料夾裡的檔案拖進 Xcode**:
   把 `CardKing/CardKing/` 底下的 `Models`、`Services`、`Views`、`Utilities` 這幾個資料夾,整個拖進 Xcode 左側的
   專案導覽器(拖到你的 App target 底下)。拖曳時 Xcode 會跳出對話框,記得勾選:
   - ✅ Copy items if needed
   - ✅ Create groups
   - ✅ 你的 App target 的 Target Membership 打勾

4. **換掉 App 入口檔**:用我提供的 `CardKingApp.swift` 取代 Xcode 自動生成的同名檔案內容
   (整個專案只能有一個 `@main`,確認沒有重複)。

4b. **換上 App 圖示**:Xcode 新專案範本會自動生成一個空的 `Assets.xcassets`,裡面已經有一個空的
    `AppIcon` 項目(左側導覽器點開 `Assets.xcassets` → `AppIcon` 就會看到一格 1024×1024 的圖示格,
    這是 Xcode 14 之後的「單一尺寸」App 圖示,拖一張圖進去它會自動產生所有需要的尺寸)。把
    `Resources/AppIcon/AppIcon-1024.png`(商務風、深藍底配金色皇冠+名片的圖示)直接拖進那一格
    (或用 Finder 開啟該圖再拖曳)即可,不需要手動處理 `Contents.json` 或建立整套圖示集——這步刻意選
    「拖進 Xcode UI」而不是用 Finder 直接置換整個 `AppIcon.appiconset` 資料夾,原因跟前面避免手刻
    `.xcodeproj` 一樣:讓 Xcode 自己管理資產目錄的內部結構,不去碰容易壞掉的地方。

4c. **加上 App 內的品牌 Logo**:進到 App 之後(名片列表畫面),導覽列原本純文字的「AcardKing」標題旁邊會多一個
    小圖示,跟主畫面的 App 圖示同一套形象。做法一樣交給 Xcode UI 處理,不手刻資產目錄:在 `Assets.xcassets`
    上按右鍵 → **New Image Set**,把新項目改名成 `Logo`(要跟 `CardListView.swift` 裡 `Image("Logo")`
    這個名字對上),然後把 `Resources/Logo/Logo-256.png` 拖進那一格(Xcode 14 之後的圖片集預設也是「單一
    尺寸」,拖一張圖進去就好,不用分別準備 1x/2x/3x)。

5. **加相機權限說明**(保險起見建議加,雖然 `VNDocumentCameraViewController` 官方文件說不強制需要):
   在 Xcode 選你的 target → **Info** 分頁 → Custom iOS Target Properties → 右鍵 Add Row,新增:
   - Key:`Privacy - Camera Usage Description`(即 `NSCameraUsageDescription`)
   - Value:例如「掃描名片需要使用相機」

5b. **加兩個檔案共享權限**(強烈建議加,尤其你是自己簽署安裝、沒有 App Store 備援的用法——這讓你之後可以
    直接用 Finder 把整個 App 的資料夾(SwiftData 資料庫+所有照片)拉出來當完整備份,細節見下面「怎麼備份最
    保險」那節):同樣在 target → **Info** 分頁新增兩個 Row:
    - Key:`Application supports iTunes file sharing`(即 `UIFileSharingEnabled`)→ Value 設 `YES`
    - Key:`Supports opening documents in place`(即 `LSSupportsOpeningDocumentsInPlace`)→ Value 設 `YES`

6. **確認 Deployment Target**:target → General → Minimum Deployments,設成 **iOS 17.0**
   (因為用了 SwiftData,這是最低要求)。

7. **簽署與安裝**:接上你的 iPhone,在 target → Signing & Capabilities 選你的 Apple ID 當 Team
   (免費帳號會顯示「Personal Team」),按 Run(⌘R)安裝到手機上。

8. **7 天後的重新簽署**:免費 Apple ID 簽的 App 每 7 天會過期,手機上會顯示「無法驗證 App」、App 打不開,
   但**這不是刪除 App、資料不會消失**——簽署過期只是「這個簽名不被信任了」,App 本體跟它的資料夾(SwiftData
   資料庫、CardImages 裡的照片)完全沒被動到。屆時只要重新接上 Xcode、Bundle Identifier 跟簽署用的 Apple
   ID/Team 都跟上次一樣,按一次 Run,就是「原地更新安裝」(跟你平常開發時重複按 Run 是同一件事),不是
   砍掉重灌,資料會直接接上,不需要你先匯出備份、重裝後再匯入。**真正會讓資料消失的情況只有**:你自己手動
   把 App 從主畫面刪除、或换了不同的 Apple ID/Team/Bundle ID 導致 iOS 把它當成「不同的 App」而生出全新的
   資料夾。只要你固定用同一個 Apple ID、同一個 Team、同一個 Bundle Identifier 重複簽署,7 天到期這件事本身
   不需要你做任何備份動作。

## 已知的不確定點 / 建議你第一次 build 時特別留意

這些是我沒辦法在這個環境實際編譯驗證、只能憑對 SwiftData/SwiftUI 的認識判斷「應該可以」的地方。如果編譯器
在這幾處報錯,通常訊息會很明確,照著改就好——先抓這幾個地方,比從頭一行一行找快很多:

- **最可能出錯的地方**:`ContactField`(電話/email 多值欄位)是存成 `[ContactField]` 陣列直接當 SwiftData
  model 的屬性,理論上 SwiftData 支援任何 `Codable` 型別這樣存,但沒有實際編譯驗證過。
- `BusinessCard` 和 `Tag` 這兩個 `@Model` class 依賴 SwiftData 自動幫它們合成 `Hashable`/`Identifiable`
  conformance(用在 `NavigationLink(value:)`、`Set<Tag>` 篩選標籤、`ForEach` 這些地方)。這是 SwiftData 官方
  範例常見的寫法,但同樣沒有實機驗證過。
- OCR 掃描後的欄位拆分(姓名/職稱/公司)是用「文字由上到下第幾行」猜的簡單啟發式,不是真的語意理解,
  準確率有限,設計上一定會讓你在存檔前手動校正,不是 100% 自動化。
- `VNDocumentCameraViewController` 官方說不需要相機權限描述,但我沒有實機驗證過,所以第 5 步還是建議加上
  以防萬一。
- vCard 匯出內嵌照片的部分,我自己刻了 vCard 規格(RFC 2426)要求的「長行折疊」邏輯(每 75 字元換行、
  接續行前面加一個空白),格式應該是對的,但沒有拿真正的 Contacts App 或其他 vCard 讀取器實際驗證過能不能
  正確顯示照片。如果匯入後照片沒顯示,人名/電話等文字欄位應該還是正常的(折疊只影響 PHOTO 那一段),
  跟我說一聲,我再調整。
- `UIFileSharingEnabled` + `LSSupportsOpeningDocumentsInPlace` 這兩個權限,官方文件說加了就會讓 App 的
  Documents 資料夾在 Finder(接 USB)、Files App 的「我的 iPhone」裡直接看到、可以拖拉複製,這是免簽署的
  個人用 App 也能用的機制(不需要上架 App Store),但我沒有實機驗證過在你的情境下(自己簽署安裝)Finder
  那邊實際顯示出來的畫面跟操作細節,如果跟預期不一樣,跟我說一聲。
- 「批次選圖建立」用的 `PHPickerViewController` 多選 + `itemProvider.loadObject(ofClass: UIImage.self)`
  讀取每張選取的照片,理論上支援相簿裡常見的格式(含 iPhone 預設的 HEIC),但我沒有實機測過選取數量很多
  (例如一次選 50 張)時的讀取速度、記憶體使用量,或某些特殊格式/來自雲端相簿尚未下載完成的照片會不會讀
  失敗。如果選了 N 張、但批次審核清單筆數對不上,通常是某幾張讀取失敗被安靜跳過了(程式碼裡是直接濾掉讀
  不到的,不會跳錯誤訊息中斷整批)——這個「安靜跳過」的行為也還沒有實機驗證過是不是你要的效果,可以之後
  改成明確提示「有 N 張讀取失敗」。
- 導覽列的品牌 Logo 靠 `Image("Logo")` 這個名字去 `Assets.xcassets` 裡找同名的 Image Set(見第 4c 步),
  這是 SwiftUI 的標準做法,但如果第 4c 步新增圖片集時取的名字跟 `Logo` 不一致,畫面上會直接看不到圖示、
  只剩「AcardKing」文字(SwiftUI 找不到對應圖片時就是留空,不會跳警告或閃退),我沒有實機驗證過這個對應
  關係,如果裝上去發現導覽列沒有圖示,先檢查圖片集的名字有沒有打對。
- **App 顯示名稱統一為「AcardKing」(2026-08-30 修正)**:App 內導覽列標題、返回按鈕文字、主畫面圖示下的
  文字,原本都是「名片王」,現在統一改成「AcardKing」——這是這個 App 的正式名字,「名片王」只是原本聊天
  裡順口用的中文說法。連帶把第 1 步 Xcode 新專案的 Product Name 也建議改成 `AcardKing`,因為那會變成裝到
  手機後主畫面圖示底下顯示的字(Bundle Display Name),這樣主畫面跟 App 內看到的名字才會一致。
- **重複名片偵測(2026-08-30 新增)**:判斷「可能是同一個人」的依據是電話號碼(只比對數字,格式不同也
  抓得到)、Email(去空白+轉小寫比對)、姓名完全相同,三者符合任一個就會跳提醒——這是啟發式比對,不是
  真的身分識別,同名同姓不同人、或同一個人換了電話都可能誤判或漏判,設計上一律讓你自己看清楚既有名片
  的資訊(何時加入、標籤等)後自己判斷要不要更新/新增/取消,不會自動幫你決定。
- **QRCodeService 用的是 CoreImage 內建的 `CIQRCodeGenerator`**,屬於系統框架,不需要額外加 Package
  依賴、不需要額外的 Capability 或權限,跟專案裡其他 Service 一樣直接引用就能用,但我沒有實機驗證過
  在真正的 iPhone 上掃描這個 QR Code 產生的內容(一段 vCard 純文字)時,系統相機 App 能不能正確辨識、
  跳出「加入聯絡人」的提示——理論上任何標準 QR reader 讀 vCard 格式都應該支援,如果實測發現掃不出來
  或帶入的欄位跟預期不同,跟我說一聲。

## 這一版做了什麼 / 沒做什麼

**有做**:
- **App 圖示 + App 內品牌 Logo(2026-08-29 新增)**:`Resources/AppIcon/AppIcon-1024.png`,商務風——深藍漸層底、
  金色皇冠疊在一張白色名片上,呼應「AcardKing」的意象。構圖刻意留了較大的安全邊界(整體約只佔畫布 8 成左右),
  不會頂到 iOS 系統套用圓角遮罩時可能裁到的邊緣。裝法見上面第 4b 步。進到 App 之後(名片列表畫面),導覽列
  也換成同一套圖示 + 「AcardKing」文字(`Views/CardListView.swift` 的導覽列 principal 項目),不再是純文字
  標題,裝法見第 4c 步——這樣主畫面圖示、App 內標題用的是同一套視覺,不會有兩套不一致的形象。
- 掃描名片:單張(相機拍正面+反面兩張,兩面都會離線辨識、自動合併成同一張名片的欄位):
  - 電話/Email:兩面聯集去重(同一支電話兩面都有只留一筆)
  - 姓名/職稱/公司/網站:**如果正反面文字不一樣(例如正面中文、反面英文翻譯,台灣名片很常見),兩個版本會用
    「/」合併保留,不會因為正面已經有值就把反面的英文版丟掉**——例如姓名可能變成「陳建豪 / Chien-Hao Chen」,
    存檔前一樣可以在審核畫面手動編修、去掉不需要的部分
  - 地址:兩面都有且不同時用「；」合併顯示
  - **統一編號(2026-08-29 新增)**:只有當那一行文字裡真的出現「統一編號」或「統編」這個字樣時才會抓,
    絕對不會把單純一串 8 位數字(可能只是沒加區碼的電話)誤判成統編——寧可少抓、也不要抓錯
  - **電話分機(2026-08-29 新增)**:如果電話那一行後面接著「分機88」「ext.88」「#88」「轉88」這種寫法,
    會把分機號碼拆到獨立的「分機」欄位;沒寫的話就是空白,不影響其他辨識
  - **部門(2026-08-29 新增,但不會自動辨識)**:名片上新增了「部門」欄位可以手動填,但因為光靠文字在第
    幾行沒辦法可靠分辨出這是部門、還是第二行職稱、還是公司名稱,寧可留白讓你自己填,也不要瞎猜——這點跟
    姓名/職稱/公司(用「第幾行文字」猜)不一樣
  - 批次掃描則是系統相機一次連拍一疊名片,只認正面(沒有反面這一步),辨識完直接進批次審核清單
  - 全程離線文字辨識,不連網路
- **批次選圖建立(2026-08-29 新增)**:除了用相機「批次掃描」,現在也可以直接從**相簿選現成的照片**——例如
  別人用 LINE/訊息傳過來的名片照片、之前存的截圖——一次選一整批,一樣會逐張跑離線 OCR、進同一個批次審核
  畫面(可整批套標籤、逐筆修正、排除不要的)。**設計上是一張照片對應一筆名片(只認正面)**,不會嘗試自動
  判斷「這兩張是同一張名片的正反面」——不管是拍照順序還是相簿選取順序,都不是可靠的依據,亂猜比留給你自己
  處理更糟;如果某張名片需要正反面都建檔,請改用「掃描一張名片(正+反面)」那個入口。
  這個功能用的是 `PHPickerViewController`(iOS 14+),官方文件說它**不需要相簿存取權限**——不用加
  `NSPhotoLibraryUsageDescription`,也不會跳權限對話框,所以第 5/5b 步沒有再多加一個相簿權限的步驟,這是
  刻意的,不是漏掉。
- 手動輸入
- 搜尋(姓名/公司/備註/標籤全文)
- 自訂標籤分類(含批次掃描時「整批套用同一組標籤」)
- 備註
- **匯出,檔案跟照片都會一起匯出**:
  - 匯出全部名片成 vCard(`.vcf`)或 CSV——vCard 版本每張名片會自動內嵌正面照片(`PHOTO` 欄位,存成
    base64,匯入 Apple 聯絡人之類的 App 會自動顯示照片);CSV 是純文字格式,沒辦法夾帶圖片,這是 CSV
    格式本身的限制,不是漏做
  - 部門會放進 vCard 的 `ORG`(公司;部門的結構化寫法)、CSV 多一個「部門」欄;統一編號沒有對應的標準
    vCard 欄位,用自訂的 `X-TAXID` 帶著走(讀不懂的 App 依規範就是直接忽略,不會壞掉,但也不保證會顯示
    出來,例如 Apple 聯絡人就不會顯示這欄),CSV 則直接多一欄「統一編號」;電話分機因為 vCard 沒有標準
    分機欄位,採用大部分通訊錄 App 的慣例,直接寫進電話那個值裡(例如「02-2712-3456 分機88」)
  - **匯出單一張名片**(名片詳情頁的「匯出這張名片」)會透過系統分享面板一次給你 vCard 檔 **+ 正面/反面的
    原始完整解析度照片**(各自獨立的檔案,不是只有內嵌在 vCard 裡的縮圖),存到 Files、AirDrop 給別人、
    傳訊息都可以,收到的人就是一個 .vcf 加最多兩張照片
- **完整備份匯出/匯入(2026-08-29 調整:照片用檔名參照,不內嵌)**:把所有名片(電話/Email/標籤/備註等全部
  文字欄位)、標籤,以及每張名片正反面照片的「檔名」(不是圖片本身的內容)打包成一個純文字 JSON 檔匯出
  (分享面板存到 Files/雲端硬碟都行)。在**同一支手機**匯入(例如 Xcode 重新簽署後)會自動把照片接回來,
  因為照片實體檔案本來就還在手機裡,只是靠檔名重新對上;換到**不同手機**匯入,文字欄位會正常還原,但照片
  要等你把實體 JPEG 檔案也搬過去(見下面「怎麼備份最保險」)才會顯示出來——細節跟原因見下一節。
- 完全離線儲存(SwiftData,存在手機本機,整個 App 沒有任何網路請求)

- **新增/編輯時的重複名片偵測 + 「我的名片」分享(2026-08-30 新增)**:
  - **確認存檔(原本就有,這次順便說明清楚)**:不管是掃描一張、批次掃描、批次選圖,還是手動輸入,存進資料庫
    的唯一入口都是 `CardFormView` 的「儲存」按鈕——掃描/批次只是幫你把欄位「猜好、預先填上」,真正寫入
    SwiftData 一定要你自己按下儲存才會發生,中途取消或跳出都不會留下任何紀錄。**批次審核這次也改成跟單張
    掃描一樣、一張一張跳出完整欄位表單**(原本是一次一張全部欄位擠在一個列表裡快速修正),每一張都是獨立的
    「確認無誤→儲存」,可以個別跳過(那一張的「取消」只跳過那一張,不會整批取消),也一樣會套用你在開始
    前選好的整批共用標籤。
  - **重複名片偵測**:新增一張名片存檔時,如果電話、Email 或姓名完全符合某張既有名片,會先跳出提醒,列出
    既有那張名片的完整資訊(公司、職稱、何時加入、最後更新、標籤、備註等)——讓你能看出「我跟這個人的關聯、
    什麼時候交流過」,再決定「更新舊名片」(用剛輸入的資料覆蓋舊的,標籤是聯集不會弄丟舊標籤)、「仍然新增
    為另一張名片」(真的是不同人,同名同姓或換了電話都有可能),或「取消,回去修改」剛才輸入的內容。編輯
    既有名片、或勾選「這是我自己的名片」時不會觸發這個檢查。
  - **我的名片(存自己的名片圖檔 + 分享)**:名片列表左上角選單新增「我的名片」,在任何一張名片的編輯畫面
    勾選「這是我自己的名片」就會被記為你自己的(同一時間只保留最新標記的一張,標記新的會自動取消舊的)。
    「我的名片」畫面會顯示這張名片的照片、一個離線產生的 QR Code(內容是這張名片的 vCard,任何相機/QR
    掃描 App 都能讀懂並帶出聯絡資訊),以及「分享名片」按鈕。**關於藍牙/NFC**:你原本要的是能用藍牙、NFC
    分享名片圖檔,但 iOS 不開放第三方 App 直接控制藍牙做「點對點名片交換」,Core NFC 也只能讀 NFC 標籤、
    不能常駐背景寫入一個可被掃描的標籤——這兩個管道對一般 App 來說技術上做不到,所以「分享名片」按的是
    系統分享面板(跟其他匯出功能一樣),AirDrop(附近的 iPhone,體感上最接近你想要的「藍牙分享」)、
    LINE、訊息、Mail、存到照片都在裡面,QR Code 則是離線、不需要對方裝置支援 AirDrop 時的備案。

**沒做(可以之後再擴充)**:直接寫入系統「聯絡人」App、批次選圖/批次掃描的正反面自動配對(理由見上)、備份
匯入時偵測並合併重複名片(現在匯入永遠是新增,同一份備份匯入兩次會變成兩份——上面新增的重複偵測只發生在
「新增一張名片」的存檔當下,還沒接到備份匯入流程裡)、真正的藍牙/NFC 點對點名片交換(技術上不可行,理由見
上)、iCloud/雲端同步(這個本來就違反你要的離線需求,故意沒做)。

## 關於「每 7 天自動備份、重裝後自動接回照片」這件事

**先修正一個前提**:第 8 步已經講了,免費 Apple ID 的 7 天簽署過期,不等於資料被刪除。你會需要重新
匯出/匯入的,其實只有兩種情況:①你自己手動刪除了 App;②你不小心用了不同的 Apple ID/Team 重簽,讓 iOS
把它當成新 App。正常「7 天到了、重新接 Xcode 按 Run」這件事本身**不需要匯出備份、也不需要匯入**——包含
照片在內的所有資料都還在原地。

**「完全自動」(不用你手動點一下)在你這個用法下做不到,而且也不需要**:這個 App 是免簽署個人安裝、完全
離線、沒有上架 App Store,不具備真正背景自動排程執行的條件(iOS 的背景任務排程本來就是「不保證準時、不
保證一定會跑」的機制,對這種安裝方式更不可靠,硬做只是把一個原本很單純的個人工具搞複雜,風險又轉嫁到一個
你沒辦法排除的地方)。所以我沒有把「每 7 天自動匯出」做進 App 裡——不是漏做,是這條路線本來就不適合你的
用法。

**怎麼備份最保險(而且幾乎不用你多做什麼)**:你本來就會每 7 天左右把手機接上 Mac、開 Xcode 重新簽署一次,
把這個動作「順便」變成完整備份的時機,比另外設計一套自動機制更可靠、也更簡單:

1. 加了第 5b 步的兩個檔案共享權限後,手機接上 Mac 時,Finder 側邊欄選你的 iPhone → 「檔案」分頁 → 找到
   CardKing → 會看到整個 App 的資料夾(SwiftData 資料庫檔案 + `CardImages` 資料夾底下所有照片)。直接把
   整個資料夾拖到 Mac 上,就是一份**連照片都在內**的完整備份,不需要另外寫程式、不需要匯出功能,你在
   Finder 裡幾秒鐘就做完了,剛好搭配你每 7 天接電腦重簽的時機一起做。
2. 如果哪天真的需要「重灌 App、匯入備份、照片自動接回來」(對應你這次問的情境):先把上一步存在 Mac 上的
   `CardImages` 資料夾整包拖回同一個 Finder 檔案分頁,把裡面的照片檔案放回手機上 App 的資料夾裡(檔名不要
   改),再用 App 裡的「從備份匯入」選你存的 JSON 檔——因為 JSON 裡每張名片記的是照片「檔名」,只要
   `CardImages` 裡有同名的檔案,匯入時就會自動對上、不用你一張一張手動重新關聯。這正是「拿掉照片備份選項,
   但在 App 裡看得到圖片、匯入時也能自動關聯」這個問題最直接的答案:JSON 負責文字資料(小、快、方便隨手
   匯出),Finder 拖拉負責照片實體檔案(大、但你本來就有機會用),兩者用檔名對上,合起來就是完整備份。
3. 如果你懶得管 Finder 這一步、也不在乎極端情況(手動刪除 App / 換簽署身分)下照片會不會保住,那就完全不用
   做任何事——正常的 7 天重簽本來就不影響任何資料,包括照片。
